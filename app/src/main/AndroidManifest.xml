<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hungrytime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"/>
        <activity android:name=".UpdatePassword" />
        <activity android:name=".PasswordActivity" />

        <activity android:name=".ProfileActivity" />
        <activity android:name=".UpdateProfile" />
        <activity android:name=".UpdateEmail" />
        <activity android:name=".RegistrationActivity" />
        <!--To Run Mohammed's code, please copy + paste the following two tags in-place of the two tags below..
            <activity android:name=".MainActivity"/>
            <activity android:name=".SplashScreen"> -->
        <activity android:name=".SplashScreen"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoadingRecipes"
            android:parentActivityName=".Categories"></activity>
        <activity android:name=".Categories" >





        <!--To Test Juwon & Malan's code, please copy + paste the following two tags in-place of the two tags above..
                <activity android:name=".SplashScreen"/>
                <activity android:name=".MainActivity"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>